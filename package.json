{
  "name": "local-cli-extension",
  "version": "0.2.0",
  "private": true,
  "description": "A configurable Chrome sidebar extension powered by local CLI and model providers.",
  "scripts": {
    "init": "node scripts/init.mjs",
    "build": "node scripts/build-extension.mjs",
    "brand": "node scripts/brand.mjs",
    "login:codex": "codex login",
    "login:cursor": "cursor-agent login",
    "start:bridge": "node bridge/server.js",
    "check": "node scripts/build-extension.mjs && node --check bridge/server.js && node --check extension/background.js && node --check extension/content.js && node --check extension/options.js"
  },
  "engines": {
    "node": ">=20"
  }
}
